Bridge

Prevents entity explosion. For example if we have 3 classes for an OS it will add 3 more for each next OS.
Instead extract implementation for each OS and use aggregation. So bridge decouples an interface from an implementation.
Stronger form of encapsulation.